# ╔══════════════════════════════════════════════════════════╗
# ║  Paperless Overconfigured — Environment Configuration   ║
# ╚══════════════════════════════════════════════════════════╝
#
# This file is auto-generated by install.sh
# Edit values here and restart: docker compose up -d

# ──────────────────────────────────────────────────────────
# CORE
# ──────────────────────────────────────────────────────────
INSTALL_DIR=${INSTALL_DIR}
PAPERLESS_ADMIN_USER=${PAPERLESS_ADMIN_USER}
PAPERLESS_ADMIN_PASSWORD=${PAPERLESS_ADMIN_PASSWORD}
PAPERLESS_SECRET_KEY=${PAPERLESS_SECRET_KEY}
PAPERLESS_TIMEZONE=${PAPERLESS_TIMEZONE}

# ──────────────────────────────────────────────────────────
# ACCESS
# ──────────────────────────────────────────────────────────
# How Paperless is accessed: tailscale | cloudflare | both | local
ACCESS_METHOD=${ACCESS_METHOD}
PAPERLESS_URL=${PAPERLESS_URL}
PAPERLESS_ALLOWED_HOSTS=${PAPERLESS_ALLOWED_HOSTS}
PAPERLESS_CORS_ALLOWED_HOSTS=${PAPERLESS_CORS_ALLOWED_HOSTS}
PAPERLESS_CSRF_TRUSTED_ORIGINS=${PAPERLESS_CSRF_TRUSTED_ORIGINS}

# Tailscale (if access method includes tailscale)
TAILSCALE_HOSTNAME=${TAILSCALE_HOSTNAME}

# Cloudflare Tunnel (if access method includes cloudflare)
PAPERLESS_DOMAIN=${PAPERLESS_DOMAIN}
CLOUDFLARE_TUNNEL_TOKEN=${CLOUDFLARE_TUNNEL_TOKEN}

# ──────────────────────────────────────────────────────────
# OCR
# ──────────────────────────────────────────────────────────
PAPERLESS_OCR_LANGUAGE=${PAPERLESS_OCR_LANGUAGE}
PAPERLESS_OCR_LANGUAGES=${PAPERLESS_OCR_LANGUAGES}

# ──────────────────────────────────────────────────────────
# AI / LLM (v3 native — paperless-ngx built-in, no side container)
# ──────────────────────────────────────────────────────────
# v3 ships with native AI. The installer supports three providers:
#
#   1) Google Gemini via OpenAI-compatible endpoint (recommended)
#      Endpoint: https://generativelanguage.googleapis.com/v1beta/openai/
#      Chat model:      gemini-2.5-flash
#      Embedding model: gemini-embedding-001
#
#   2) OpenAI (native)
#      Endpoint: https://api.openai.com/v1/
#      Chat model:      gpt-4o-mini
#      Embedding model: text-embedding-3-small
#
#   3) Ollama (local, private)
#      Endpoint: http://host.docker.internal:11434
#      Chat model:      llama3.1
#      Embedding model: nomic-embed-text
#
# Set PAPERLESS_AI_ENABLED=false to disable.
PAPERLESS_AI_ENABLED=${PAPERLESS_AI_ENABLED}
PAPERLESS_AI_LLM_BACKEND=${PAPERLESS_AI_LLM_BACKEND}
PAPERLESS_AI_LLM_MODEL=${PAPERLESS_AI_LLM_MODEL}
PAPERLESS_AI_LLM_API_KEY=${PAPERLESS_AI_LLM_API_KEY}
PAPERLESS_AI_LLM_ENDPOINT=${PAPERLESS_AI_LLM_ENDPOINT}
PAPERLESS_AI_LLM_OUTPUT_LANGUAGE=${PAPERLESS_AI_LLM_OUTPUT_LANGUAGE}
PAPERLESS_AI_LLM_REQUEST_TIMEOUT=${PAPERLESS_AI_LLM_REQUEST_TIMEOUT}
PAPERLESS_AI_LLM_EMBEDDING_BACKEND=${PAPERLESS_AI_LLM_EMBEDDING_BACKEND}
PAPERLESS_AI_LLM_EMBEDDING_MODEL=${PAPERLESS_AI_LLM_EMBEDDING_MODEL}
PAPERLESS_AI_LLM_EMBEDDING_ENDPOINT=${PAPERLESS_AI_LLM_EMBEDDING_ENDPOINT}

# Paperless version pin (v3 has breaking DB migrations — do not use :latest)
PAPERLESS_VERSION=${PAPERLESS_VERSION}

# ──────────────────────────────────────────────────────────
# NEO4J GRAPH DATABASE
# ──────────────────────────────────────────────────────────
ENABLE_GRAPH=${ENABLE_GRAPH}
NEO4J_PASSWORD=${NEO4J_PASSWORD}

# ──────────────────────────────────────────────────────────
# DATABASE
# ──────────────────────────────────────────────────────────
POSTGRES_DB=paperless
POSTGRES_USER=paperless
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# ──────────────────────────────────────────────────────────
# EMAIL (optional — for email-to-paperless ingestion)
# ──────────────────────────────────────────────────────────
PAPERLESS_EMAIL_HOST=${PAPERLESS_EMAIL_HOST}
PAPERLESS_EMAIL_PORT=${PAPERLESS_EMAIL_PORT}
PAPERLESS_EMAIL_HOST_USER=${PAPERLESS_EMAIL_HOST_USER}
PAPERLESS_EMAIL_HOST_PASSWORD=${PAPERLESS_EMAIL_HOST_PASSWORD}
PAPERLESS_EMAIL_USE_SSL=${PAPERLESS_EMAIL_USE_SSL}
PAPERLESS_EMAIL_FROM=${PAPERLESS_EMAIL_FROM}

# ──────────────────────────────────────────────────────────
# BACKUPS
# ──────────────────────────────────────────────────────────
ENABLE_BACKUPS=${ENABLE_BACKUPS}
ENCRYPTION_PASSPHRASE=${ENCRYPTION_PASSPHRASE}
HEALTHCHECK_URL=${HEALTHCHECK_URL}
GDRIVE_REMOTE=${GDRIVE_REMOTE}
GDRIVE_BACKUP_DIR=${GDRIVE_BACKUP_DIR}
GITHUB_BACKUP_REPO=${GITHUB_BACKUP_REPO}

# ──────────────────────────────────────────────────────────
# DOCKER COMPOSE PROFILES
# ──────────────────────────────────────────────────────────
# Automatically set by installer. Comma-separated list.
# Possible values: ai, tunnel, graph
COMPOSE_PROFILES=${COMPOSE_PROFILES}
